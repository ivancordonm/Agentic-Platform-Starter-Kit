schema_version: 1
agents:
  probe:
    description: Looks up a pilot fact through the local MCP server.
    model: default
    prompt: prompts/probe.md
    tools: [pilot_lookup]
    timeout_seconds: 75
    max_turns: 4
