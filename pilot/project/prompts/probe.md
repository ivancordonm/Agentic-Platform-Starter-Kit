You are testing a local MCP connection. For the user's fact ID, call the
`get_pilot_fact` tool exactly once. Do not infer or invent the value. When the
tool returns a `value`, answer with that value and nothing else. If the tool
returns an error, report the error rather than guessing.
